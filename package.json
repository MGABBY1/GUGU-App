{
  "name": "gugu-app",
  "version": "2.0.0",
  "private": true,
  "description": "Gura & Gurisha App — local marketplace for Rwanda",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix shared && npm install --prefix services/gateway && npm install --prefix services/auth-service && npm install --prefix services/listings-service && npm install --prefix services/chat-service && npm install --prefix services/users-service",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:gateway": "npm run dev --prefix services/gateway",
    "dev:auth": "npm run dev --prefix services/auth-service",
    "dev:listings": "npm run dev --prefix services/listings-service",
    "dev:chat": "npm run dev --prefix services/chat-service",
    "dev:users": "npm run dev --prefix services/users-service",
    "build:frontend": "npm run build --prefix frontend",
    "start:gateway": "npm run start --prefix services/gateway"
  }
}
